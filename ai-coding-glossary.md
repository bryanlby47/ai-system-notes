# AI Coding 常见名词

这是一份面向 AI Coding 初学者的速查表。重点不是背定义，而是知道一个名词属于哪一层、解决什么问题，以及出错时应该往哪里查。

## 1. Linux、终端与 Git

### 终端与系统

| 名词 | 通俗解释 |
| --- | --- |
| Terminal（终端） | 输入命令、查看程序输出的窗口。 |
| Shell | 解释并执行命令的程序；`zsh`、`bash` 都是 Shell。 |
| CLI | Command Line Interface，命令行界面。 |
| 环境变量 | 由操作系统或 Shell 提供的配置值，如 API Key、运行环境和程序路径。 |
| `PATH` | 告诉系统去哪些目录寻找可执行程序的环境变量。 |
| `sudo` | 临时以管理员权限执行命令；滥用可能导致文件权限混乱。 |
| SSH | 安全连接远程机器或 Git 仓库的协议。 |
| `ssh-keygen` | 创建 SSH 公钥和私钥的命令。 |
| 进程 | 正在运行的程序实例。 |
| 端口 | 本机服务对外提供连接的编号，如前端开发服务常用 `3000`。 |
| `localhost` | 当前这台电脑，通常对应 `127.0.0.1`。 |

### Git 与 GitHub

| 名词 | 通俗解释 |
| --- | --- |
| Git | 在本地记录文件版本和协作历史的版本控制工具。 |
| GitHub | 托管 Git 仓库并提供协作、Issue、PR、CI 等功能的平台。 |
| Repository（仓库） | 一个项目的文件及其版本历史。 |
| `git init` | 把当前目录初始化为 Git 仓库。 |
| `git clone` | 把远程仓库复制到本地。 |
| Working Tree | 当前实际看到和编辑的文件。 |
| Staging Area | 暂存区；决定下一次提交包含哪些改动。 |
| `git add` | 把指定改动放入暂存区。 |
| Commit | 一次带说明的版本快照。 |
| Branch | 一条独立开发线，如 `main`、`feature/login`。 |
| Remote | 远程仓库地址，默认名称通常是 `origin`。 |
| `git push` | 把本地提交发送到远程仓库。 |
| `git pull` | 获取远程改动并整合到当前分支。 |
| Merge | 合并两条分支的历史。 |
| Rebase | 把一组提交重新接到另一个提交之后，使历史更线性。 |
| Merge Conflict | Git 无法自动判断两处冲突改动该保留哪一个。 |
| Detached HEAD | 当前指向某个具体提交，而不是正常分支；此时新提交可能不容易找到。 |
| `.gitignore` | 声明不应被 Git 跟踪的文件，如密钥、依赖和构建产物。 |

常见问题：推送权限或 Token 过期、分支混乱、冲突不会解决、误提交 `.env` 或大文件、Rebase 后不知道如何恢复。

## 2. 依赖、运行环境与容器

| 名词 | 通俗解释 |
| --- | --- |
| Runtime（运行时） | 程序真正运行所依赖的环境，如 Node.js、Python。 |
| Package Manager | 安装和管理依赖的工具，如 npm、pnpm、pip、Homebrew。 |
| `package.json` | Node.js 项目的依赖、脚本和项目元数据。 |
| `node_modules` | Node.js 依赖实际安装的位置，通常很大且不提交到 Git。 |
| Lockfile | 锁定依赖的精确版本，如 `package-lock.json`、`pnpm-lock.yaml`。 |
| SemVer | 语义化版本规则，常见形式为 `主版本.次版本.补丁版本`。 |
| `^` / `~` | 依赖版本范围；`^` 通常允许更新次版本，`~` 通常只允许更新补丁版本。 |
| Virtual Environment | 为单个 Python 项目隔离依赖的环境。 |
| `.env` | 本地保存环境变量的常见文件，不应提交真实密钥。 |
| Docker Image | 打包应用及其运行环境的只读模板。 |
| Container | Docker Image 启动后的运行实例。 |
| Dockerfile | 描述如何构建 Docker Image 的文件。 |
| Volume | 在容器外持久保存或共享数据的机制。 |
| Port Mapping | 把容器端口映射到宿主机端口。 |

常见问题：安装依赖卡住、Node 或包管理器版本不一致、Lockfile 冲突、原生依赖编译失败、环境变量遗漏、端口被占用、Docker 容器未启动或端口未映射。

## 3. 前端基础

### HTML 与 CSS

| 名词 | 通俗解释 |
| --- | --- |
| HTML | 定义网页内容和结构。 |
| DOM | 浏览器把 HTML 转成的可操作对象树。 |
| CSS | 控制页面的布局和视觉样式。 |
| Selector | 选择要应用样式的元素。 |
| Class / ID | 标记元素的属性；Class 可复用，ID 应在页面中唯一。 |
| Specificity | 多条 CSS 规则冲突时的优先级计算方式。 |
| Box Model | 元素由内容、内边距、边框和外边距组成。 |
| Flexbox | 适合一维排列和对齐的布局方式。 |
| Grid | 适合二维行列布局的方式。 |
| Position | 控制元素的定位方式，如 `relative`、`absolute`、`fixed`。 |
| `z-index` | 控制重叠元素的前后层级，但会受层叠上下文影响。 |
| Media Query | 根据屏幕尺寸或设备特征应用不同样式。 |
| Viewport | 浏览器中页面可见区域；移动端通常需要设置 viewport。 |
| Responsive Design | 让同一页面适应手机、平板和桌面屏幕。 |

### JavaScript 与框架

| 名词 | 通俗解释 |
| --- | --- |
| JavaScript | 浏览器和 Node.js 中常用的编程语言。 |
| Event Listener | 在点击、输入等事件发生时执行代码。 |
| Callback | 作为参数传给其他函数、稍后执行的函数。 |
| Promise | 表示一个将来成功或失败的异步结果。 |
| `async` / `await` | 以接近同步代码的写法组织异步操作。 |
| `try` / `catch` | 捕获并处理运行错误。 |
| `fetch` | 浏览器内置的 HTTP 请求 API。 |
| CORS | 浏览器的跨域访问规则，需要服务端返回正确响应头。 |
| React / Vue | 用组件和状态构建用户界面的前端框架。 |
| Component | 可复用的界面单元。 |
| Props | 父组件传给子组件的只读数据。 |
| State | 会影响界面显示、并随交互变化的数据。 |
| `useEffect` | React 中处理请求、订阅等副作用的 Hook。 |
| Vite / Webpack | 打包、转换和启动前端开发环境的工程工具。 |

常见问题：CSS 权重判断错误、布局被 padding 或 overflow 破坏、移动端适配失效、异步执行顺序混乱、请求失败未处理、`useEffect` 无限循环、列表缺少稳定 Key、定时器或订阅未清理。

## 4. 后端、API 与数据库

### 后端与 API

| 名词 | 通俗解释 |
| --- | --- |
| Backend（后端） | 处理业务规则、权限、数据和外部服务的服务器程序。 |
| API | 软件之间约定好的调用接口。 |
| REST | 以资源和 HTTP 方法组织 API 的常见风格。 |
| Endpoint | 一个具体 API 地址，如 `POST /api/users`。 |
| Request / Response | 客户端发送的请求和服务端返回的响应。 |
| HTTP Method | 请求动作，如 GET、POST、PUT、PATCH、DELETE。 |
| Status Code | HTTP 结果编号，如 200 成功、404 未找到、500 服务端错误。 |
| JSON | 前后端传递结构化数据的常见文本格式。 |
| Middleware | 在请求到达核心业务逻辑前后执行的通用处理层。 |
| Webhook | 某个事件发生后，由一个系统主动通知另一个系统。 |
| WebSocket | 允许客户端和服务端保持双向实时通信的协议。 |

### 数据库

| 名词 | 通俗解释 |
| --- | --- |
| Database | 持久保存和查询数据的系统。 |
| SQL | 操作关系型数据库的查询语言。 |
| Table / Row / Column | 关系型数据库中的表、记录和字段。 |
| Primary Key | 唯一标识一条记录的字段。 |
| Foreign Key | 表示两张表之间关联关系的字段约束。 |
| Schema | 数据结构、字段类型和约束的定义。 |
| Query | 对数据库发出的读取或修改指令。 |
| Index | 用额外存储换取更快查询的数据结构。 |
| Transaction | 把多步数据库操作作为一个整体成功或失败。 |
| Migration | 以可追踪方式修改数据库结构。 |
| ORM | 用编程语言对象操作数据库的工具。 |
| NoSQL | 非关系型数据库的统称，常见类型有文档、键值和图数据库。 |
| Cache | 保存常用结果以减少重复计算或数据库查询。 |
| Vector Database | 按向量相似度存储和检索内容，常用于语义搜索和 RAG。 |

常见问题：字段类型不一致、Migration 未执行、查询缺少索引、N+1 查询、事务边界错误、缓存数据过期、开发与生产数据库配置混用。

## 5. 安全与权限

| 名词 | 通俗解释 |
| --- | --- |
| Authentication | 认证：确认“你是谁”，如登录。 |
| Authorization | 授权：确认“你能做什么”。 |
| API Key | 调用服务时用于识别项目或调用方的秘密凭据。 |
| Secret | 密码、Token、私钥等必须保密的信息。 |
| Token | 表示身份或访问权限的一段凭据。 |
| OAuth | 让用户授权第三方应用访问部分资源的标准流程。 |
| Least Privilege | 最小权限原则：只授予完成任务所需的权限。 |
| Encryption | 把数据转换为没有密钥就难以读取的形式。 |
| Hash | 将数据单向映射为固定长度结果，常用于校验或密码存储。 |
| SQL Injection | 把恶意 SQL 混入输入，操纵数据库查询。 |
| XSS | 把恶意脚本注入网页并在其他用户浏览器中执行。 |
| CSRF | 利用用户已登录状态，诱导浏览器发出非本人意愿的请求。 |
| Prompt Injection | 用恶意输入诱导模型忽略原任务、泄露信息或执行危险操作。 |
| Sandbox | 限制代码或 Agent 可访问资源的隔离环境。 |

常见问题：把密钥写进代码或提交到 Git、只做前端权限判断、日志泄露敏感信息、依赖版本存在漏洞、Agent 工具权限过大、未经确认执行删除或发布操作。

## 6. LLM 与生成式 AI

| 名词 | 通俗解释 |
| --- | --- |
| LLM | Large Language Model，大语言模型。 |
| Prompt | 给模型的指令和上下文。 |
| Token | 模型处理文本的基本单位，不完全等同于字或单词。 |
| Context Window | 一次请求中模型能接收和生成的 Token 总量范围。 |
| System Prompt | 定义模型角色、规则和边界的高优先级指令。 |
| Temperature | 控制输出随机程度的采样参数。 |
| Hallucination | 模型生成看似合理、实际错误或无依据的内容。 |
| Embedding | 把文本、图片等转换为可比较的数字向量。 |
| Semantic Search | 根据语义相似度而不是只按关键词搜索。 |
| RAG | 先检索外部资料，再让模型基于资料生成回答。 |
| Fine-tuning | 用特定数据继续训练模型，使其更适合某类任务或风格。 |
| Inference | 使用训练完成的模型生成结果的过程。 |
| Structured Output | 要求模型按 JSON Schema 等固定结构返回结果。 |
| Multimodal | 同时理解或生成文本、图片、音频、视频等多种信息。 |

## 7. Agent 与 AI Coding

| 名词 | 通俗解释 |
| --- | --- |
| AI Coding Assistant | 提供补全、解释、生成或修改代码能力的 AI 助手。 |
| Coding Agent | 能读取项目、编辑文件、运行命令、测试并根据结果继续行动的 Agent。 |
| Agent | 围绕目标反复观察、决策、行动并处理反馈的系统。 |
| Tool Calling | 模型以结构化参数请求外部工具执行操作。 |
| MCP | Model Context Protocol，连接模型与工具、数据源的开放协议。 |
| Skill | 可复用的任务说明、工作流程和配套资源。 |
| Workflow | 预先定义、较确定的执行步骤。 |
| Agent Loop | “观察 → 决策 → 行动 → 获取反馈”的循环。 |
| Harness | 为 Agent 提供文件、终端、浏览器、测试和沙箱等执行能力的环境。 |
| Agent Runtime | 管理上下文、状态、工具、权限、循环和恢复的软件系统。 |
| Context Engineering | 设计模型在当前步骤应该看到哪些指令、资料和状态。 |
| State | 当前任务进度、变量、结果和决策等可恢复信息。 |
| Memory | 跨步骤或跨任务保存、检索长期信息的机制。 |
| Checkpoint | 保存可恢复的任务节点，失败或中断后可以继续。 |
| Router | 根据任务类型、成本或能力选择模型、Agent 或流程。 |
| Orchestrator | 协调多个 Agent、工具和工作流的组件。 |
| Human in the Loop | 在关键决策、审批或高风险动作中让人参与。 |
| Evaluation（Evals） | 用测试集、指标或人工评审衡量 AI 系统质量。 |
| Guardrail | 限制不安全、不合规或偏离目标行为的规则和机制。 |

## 8. Vibe Coding

Vibe Coding 通常指主要通过自然语言描述意图，让 AI 快速生成和修改代码，再通过运行结果不断迭代的开发方式。它适合原型、探索和低风险工具，但“看起来能跑”不等于已经正确、安全、可维护。

### 常见问题

- **需求没有边界**：一句模糊指令让 AI 自行补全大量产品和技术决策，结果偏离真实目标。
- **不理解生成代码**：无法判断错误、安全风险和后续维护成本。
- **一次改动过大**：同时改太多文件，难以审查，也难以定位回归原因。
- **只看页面，不跑测试**：表面正常，但边界条件、数据和权限流程已经损坏。
- **反复打补丁**：没有先定位根因，修复相互覆盖，代码越来越复杂。
- **上下文污染**：把无关日志、旧需求或错误假设持续塞给模型，导致判断失焦。
- **版本与环境漂移**：AI 使用了过时 API，或本地、CI、生产环境的依赖不一致。
- **敏感信息泄露**：把密钥、客户数据或内部代码发给不合适的服务，或提交到仓库。
- **盲目执行命令**：未查看范围就删除文件、改数据库、推送代码或部署生产环境。
- **缺少版本控制**：没有小步提交，出错后难以比较和回退。
- **忽略非功能需求**：功能能用，但性能、可访问性、监控、成本和安全没有考虑。
- **把模型当权威**：模型可能编造库、参数、错误原因或“已经完成”的验证结果。

### 更稳妥的工作方式

1. 先写清目标、约束、不做什么和验收标准。
2. 要求 AI 先检查现有代码与项目规则，再提出最小改动方案。
3. 把任务拆成可验证的小步，每一步都检查差异和运行结果。
4. 对依赖版本、API、报错和安全结论查阅官方资料。
5. 对数据库迁移、权限、删除、支付、发布等高风险动作保留人工确认。
6. 让测试、类型检查、Lint、构建和实际交互共同证明“完成”。
7. 用 Git 保留清晰的小提交，确保出现问题时可以定位和恢复。

## 9. 遇到问题时先判断属于哪一层

```text
页面显示不对       → HTML / CSS / 浏览器
点击后没有反应     → JavaScript / 状态 / 事件
请求发不出去       → 网络 / URL / CORS / 环境变量
接口返回错误       → 后端 / 权限 / 数据校验
数据不对或很慢     → 数据库 / 查询 / 索引 / 缓存
本地能跑线上失败   → 依赖 / 配置 / 构建 / 部署环境
AI 回答不可靠      → Prompt / Context / 数据来源 / Evals
Agent 做错了动作   → Tool / State / 权限 / Guardrail / 审批
代码越修越乱       → 任务范围 / 根因分析 / 测试 / Git 历史
```

一个实用原则是：先收集报错、复现步骤和改动范围，再让 AI 提出假设；不要只把“坏了”交给 AI 猜。
